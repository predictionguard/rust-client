version: 2
jobs:
  build:
    docker:
      - image: cimg/rust:1.78.0
    steps:
      - checkout
      - run:
          name: Check versions
          command: |
              cargo --version
      - run:
          name: Build
          command: |
              cargo build
